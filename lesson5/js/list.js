      button.onclick = function() {
        let myItem = input.value;
        input.value = '';

       istBtn.onclick = function(e) {
          list.removeChild(listItem);
        }

        input.focus();
      }
